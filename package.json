{
  "name": "api-gateway-dynamodb-proxy",
  "description": "Demo for API Gateway with DynamoDB proxy service",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "deploy": "serverless deploy",
    "destroy": "serverless remove"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.142",
    "aws-lambda": "^1.0.7",
    "serverless": "^4.1.17"
  }
}
